from django.apps import AppConfig


class ShopConfig(AppConfig):
    """This class is responsible for registering the application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'shop'
